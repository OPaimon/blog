---
layout: layouts/base.vto
title: "派蒙的小窝"
description: "记录前端开发、编程学习与日常思考的个人博客。"
---

<h1 class="m-0 text-2xl font-semibold">派蒙的小窝</h1>
<p class="m-0 opacity-80">最近文章</p>

<!-- 
  使用 Lume 内置的 search 工具直接获取文章列表
  - "type=post" 匹配你的文章（对应你在 _config.ts 或 posts/_data.yml 里的定义）
  - "date=desc" 自动按发布日期倒序排列
-->
{{ set posts = search.pages("type=post draft!=true", "date=desc") }}

{{ if posts.length }}
  <ul class="m-0 p-0 list-none space-y-3">
    {{ for post of posts }}
      <li class="leading-relaxed">
        <div>
          <a
            class="font-semibold no-underline hover:underline"
            href="{{ post.url }}"
          >
            {{ post.title }}
          </a>
          <span class="opacity-70"> — </span>
          <time
            class="text-sm opacity-70"
            datetime="{{ post.date |> date('yyyy-MM-dd') }}"
          >
            {{ post.date |> date("yyyy-MM-dd") }}
          </time>
        </div>
        {{ if post.description }}
          <div class="text-sm opacity-80">{{ post.description }}</div>
        {{ /if }}
      </li>
    {{ /for }}
  </ul>
{{ else }}
  <p class="m-0 opacity-80">暂时还没有文章。</p>
{{ /if }}
